name: Fetch New Arrivals (all brands)

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: fetch-lululemon
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "curl_cffi>=0.7.0" "playwright>=1.46,<2"

      - name: Install Playwright Chromium
        run: python -m playwright install --with-deps chromium

      - name: Run Lululemon scraper
        run: python scripts/fetch_lululemon.py
        continue-on-error: true

      - name: Run all-brands scraper (alo/adan/dfyne/tala/blakely/gym)
        if: ${{ always() }}
        run: python scripts/fetch_all_brands.py
        continue-on-error: true

      - name: Commit & push if changed
        if: always()
        run: |
          set -e

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          files="lululemon-new.json alo.json adan.json dfyne.json tala.json blakely.json gym.json"
          tmp_dir="/tmp/new-arrivals-json"
          rm -rf "$tmp_dir"
          mkdir -p "$tmp_dir"

          for f in $files; do
            if [ -f "$f" ]; then
              cp "$f" "$tmp_dir/$f"
              echo "saved: $f"
            else
              echo "skip missing: $f"
            fi
          done

          git checkout -- .
          git clean -fd

          git fetch origin main
          git checkout -B main origin/main

          for f in $files; do
            if [ -f "$tmp_dir/$f" ]; then
              cp "$tmp_dir/$f" "$f"
              git add "$f"
              echo "added: $f"
            fi
          done

          if git diff --staged --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "chore: update new-arrivals JSON ($(date -u +'%Y-%m-%d %H:%M UTC'))"

          if git push origin main; then
            echo "Push succeeded."
            exit 0
          fi

          echo "Push rejected, retrying..."
          rm -rf "$tmp_dir"
          mkdir -p "$tmp_dir"

          for f in $files; do
            [ -f "$f" ] && cp "$f" "$tmp_dir/$f"
          done

          git fetch origin main
          git reset --hard origin/main
          git clean -fd

          for f in $files; do
            if [ -f "$tmp_dir/$f" ]; then
              cp "$tmp_dir/$f" "$f"
              git add "$f"
            fi
          done

          if git diff --staged --quiet; then
            echo "No changes after retry."
            exit 0
          fi

          git commit -m "chore: update new-arrivals JSON ($(date -u +'%Y-%m-%d %H:%M UTC'))"
          git push origin main
